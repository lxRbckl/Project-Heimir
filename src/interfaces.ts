export interface RepositoryDetails {
  readme: string;
  branchCount: number;
  repositoryUrl: string;
}

export interface RepositoryInfo {
  readme: string;
  branchCount: number;
  repositoryUrl: string;
}
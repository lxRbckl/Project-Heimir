export interface RepositoryDetails {
  title: string;
  stack: string;
  iteration: number;
  description: string;
  url: string;
}

export interface RepositoryInfo {
  readme: string;
  branchCount: number;
  url: string;
}